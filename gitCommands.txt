I want to commit and push my changes
------------------------------
git commit -am "commit message"

	Update branch with master
	---------------	
	git checkout master
	git pull origin master
	git checkout <branch>
	git merge master

git push origin <name of branch>


Making a new branch
------------------------------
git checkout -b <branch to get(typically master)> <new branch name>

made a new file and want to commit it
------------------------------
git add <file name>

git status
